# Git
.git
.gitignore

# Python
__pycache__
*.py[cod]
*.pyo
*.egg-info
build/
dist/
wheels/
.pytest_cache
.mypy_cache
.ruff_cache

# 虚拟环境（容器里会重建）
.venv

# 本地数据 & 日志（会挂载为 volume）
logs/
sqlite.db
*.db
*.sqlite*

# 环境变量（容器通过 env_file 注入，不打进镜像）
.env
.env.*

# IDE / OS
.idea
.vscode
.DS_Store
*.swp

# Docker 自身
Dockerfile
docker-compose.yml
.dockerignore

# 其它不需要进镜像的
Makefile
node_modules
requirements.txt
